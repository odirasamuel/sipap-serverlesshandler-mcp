# Lambda Layer Dependencies for sipap-serverlesshandler-mcp (sipap-mcp)
# These are the runtime dependencies that will be packaged into the Lambda layer

# Core SIPAP dependency
sipap-common>=0.1.0

# MCP server framework dependencies
pydantic>=2.7.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
jsonschema>=4.22.0
typing-extensions>=4.12.0

# Additional runtime dependencies from sipap-common layer
# (pyyaml, jinja2, boto3, redis, sqlalchemy)
# These will be inherited from sipap-common layer, but we include them here
# to ensure sipap-mcp layer can be used independently if needed
