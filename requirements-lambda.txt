# Lambda Layer Dependencies for sipap-serverlesshandler-mcp (sipap-mcp)
# These are the runtime dependencies that will be packaged into the Lambda layer
#
# IMPORTANT: sipap-common is provided as a SEPARATE Lambda layer and should NOT
# be included here. Both layers (common + serverlesshandler) are attached to
# Lambda functions independently following the Sentinel two-layer pattern.

# MCP server framework dependencies
pydantic>=2.7.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
jsonschema>=4.22.0
typing-extensions>=4.12.0
